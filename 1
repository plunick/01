# Операции с числами: округление до десятых, возведение в степень, модуль числа, деление с остатком, целочисленное деление
x = float(input())  # Вводим число
print(round(x, 1))  # Округлил число до десятых

x = 2
y = 4
print(x ** y)  # Возводим число в степень

c = 100
d = 55
p = abs(d - c)  # Модуль числа
print(p)

print(7 / 2)  # Деление с остатком
print(7 // 2)  # Целочисленое деление

# Взаимодействия со списками: дубликация списка, повторение списка, вызов элемента списка по индексу, полная очистка списка, подсчёт определенного элемента в списке
a = [1, 2, 3]
b = a.copy()  # Дубликация списка "a" в список "b"
print(b)

x = ["Инфа"] * 5  # Повторение списка
print(x)

x = ["qwe", "zxc", "r"]  # Вызов элементов по индексу
print(x[0])
print(x[1])
print(x[2])

x = ["qwe", "zxc", "r"]  # Очистка списка
print(x.clear())

x = ["qwe", "zxc", "r", "qwe", "r", "r"]  # Подсчёт определенного элемента в списке
xcount = x.count("r")
print(xcount)

# Взаимодействие с строками: конкатенация строк, дублирование строк, подсчет длины строки, срез, вывод строки в верхнем регистре.
x = 'pyt'
y = 'hon'
print(x + y)  # Сложение строк(конкатенация)

x = 'Python'
print(x * 2)  # Дублирование строки

x = len('Python')
print(x)  # Длина строки

x = 'Python'
print(x[:2])  # Срез с начала по 2 элемент строки

x = "Python"
print(x.upper())  # Вывод строки в верхнем регистре
